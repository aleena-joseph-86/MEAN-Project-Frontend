import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {

  constructor() { }

  login() {
    // logic to fetch and check the login credentials into database 
  }

  register() {
    // logic to enter the data into the database
  }
}
